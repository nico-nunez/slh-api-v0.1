<div class="row">
	<div class="col-lg-8 col-md-10 col-sm-12 m-auto">
		<div class="card text-center">
      <div class="row mt-3">
        <form action="/parties/example" method="POST">
          <button class="btn btn-primary mb-3">Get Selections *</button>
        </form>
        <div class="col-6">
          <h2>Selector *</h2>
				</div>
				<div class="col-6">
          <h2>Selected *</h2>
				</div>
			</div>
			<div class="card-body">
				<ul class="list-group">
					<% for (const selection of selections) { %>
					<li class="list-group-item">
						<div class="row">
							<div class="col-6">
								<p class="m-0"><%= selection.selector.displayName %></p>
							</div>
							<div class="col-6">
								<p class="m-0"><%= selection.recipient.displayName %></p>
							</div>
						</div>
					</li>
					<% } %>
				</ul>
				<div class="text-start mt-3">
					<p class="text-muted text-small mb-0">
						* This is shown for demostrative purposes only:
					</p>
					<p class="text-muted text-small mb-0">
						- All secret santa selections are randomly generated on the specified date.
					</p>
					<p class="text-muted text-small mb-0">
						- Your selection will appear next to its associated party in your
						dashboard.
					</p>
					<p class="text-muted text-small mb-0">
						- No one (including party creator) can see your selection or any
						other.
					</p>
				</div>
			</div>
		</div>
	</div>
</div>
